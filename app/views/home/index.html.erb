<h1>Welcome to book-A-doc</h1>
<p>You're easy way to medical hapiness.</p>


<% if user_signed_in? %>
<% if !current_user.patient_profile && (current_user.has_role? :patient) %>


<%= link_to 'create your profile', new_patient_profile_path, class: 'btn btn-info' %>
<% end %>

<% if (current_user.has_role? :doctor) && !current_user.doctor_profile %>
<%= link_to 'Create your doctor profile', new_doctor_profile_path, class: 'btn btn-info' %>



<% end %>
<h3> Make and appotiment with:</h3>
<% DoctorProfile.all.each do |doctor_profile| %>
<%= link_to doctor_profile.doctor_identity, new_appointment_path(doctor: doctor_profile.user_id), class: 'btn btn-info' %>

<% end %>

<% else %>


<%= link_to 'Register to make your appoitment', new_user_registration_path(user_type: 'patient'), class: 'btn btn-primary' %>

<% end %>
